"""
ФАЙЛ: check_installation.py
НАЗНАЧЕНИЕ: Проверяет, правильно ли установлены все библиотеки
"""

# Список библиотек для проверки
библиотеки = {
    'numpy': 'np',
    'matplotlib': 'plt', 
    'trimesh': 'trimesh',
    'scipy': 'scipy',
    'pyvista': 'pv'
}

print("=== ПРОВЕРКА УСТАНОВКИ БИБЛИОТЕК ===\n")

все_установлены = True

for имя_библиотеки, псевдоним in библиотеки.items():
    try:
        # Пытаемся импортировать библиотеку
        if имя_библиотеки == 'matplotlib':
            __import__('matplotlib')
        else:
            __import__(псевдоним)
        print(f"✓ {имя_библиотеки} - УСТАНОВЛЕНА")
    except ImportError:
        print(f"✗ {имя_библиотеки} - НЕ УСТАНОВЛЕНА")
        print(f"  Установите командой: pip install {имя_библиотеки}")
        все_установлены = False

if все_установлены:
    print("\n✓ ВСЕ БИБЛИОТЕКИ УСТАНОВЛЕНЫ! Можно начинать работу.")
else:
    print("\n✗ НЕКОТОРЫЕ БИБЛИОТЕКИ ОТСУТСТВУЮТ. Установите их перед продолжением.")